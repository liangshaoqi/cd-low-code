import React, { FC } from 'react';

type PropsType = {};
const ConponentContainer: FC<PropsType> = (props: PropsType) => {
//  const {} = props
  return (
    <div>
      组件父容器
    </div>
  );
};
export default ConponentContainer